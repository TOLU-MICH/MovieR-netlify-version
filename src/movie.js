import { getData, swiper } from "../dist/modules/main.js";
import { nav, myFunction, collaps } from "../dist/modules/nav.js";

const links = ["./movie.html", "../series/series.html"];
nav(), myFunction(), collaps(), getData("movie", links);
swiper;
